# JavaScript Detection Rules

Signals:
- `.js` and/or `.jsx` exist in non-test folders
- `package.json` exists
- If TS signals present, prefer TypeScript rules instead

React signals (for JS):
- `react` dependency exists OR `.jsx` present OR JSX syntax present

> See: references/routing.md
